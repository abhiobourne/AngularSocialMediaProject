// user-document.interface.ts

// Define the UserDocument interface
export interface UserDocument {
    publicName: string; // The user's public name
    description: string; // The user's description or bio
    // Add more properties as needed
  }
  